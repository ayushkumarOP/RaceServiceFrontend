# Public RaceService backend URL. This is baked into the Vite bundle at build time.
VITE_API_URL=https://api.example.com

# ForumService base URL. This is baked into the Vite bundle at build time.
VITE_FORUM_API_URL=https://forumservice-942724250878.asia-south1.run.app
